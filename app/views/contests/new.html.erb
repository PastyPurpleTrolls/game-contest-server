<% setTitle("Add Contest") %>
<br>
<ol class="breadcrumb">
    <li><a href="<%= root_path %>">Home</a></li>
    <li><a href="<%= contests_path %>">Contests</a></li>
    <li class="active">New Contest</li>
</ol>
<div class="row">
    <div class="center display-container col-md-offset-2 col-lg-offset-3 col-md-8 col-lg-6">
        <h1 class="headings new-heading left row">New Contest</h1>
        <%= form_for @contest do |f| %>
            <%= render 'shared/error_messages', object: @contest %>
            <div class="form-group row left">
                <div class="col-xs-4">
                    <%= f.label :referee_id, 'Referee:' %>
                </div>
                <%= f.collection_select(:referee_id, @referees, :id, :name, {prompt: "Select a Referee"}, class: 'form-input form-element col-xs-4') %>
                <div class="col-xs-4 xs-font info-link left-padding">
                OR <a href="/referees/new">Create New Referee</a>
                </div>
            </div>
            <%= render 'fields', f: f %>
            <%= f.submit 'Create Contest', class: 'btn btn-danger' %>
        <% end %>
    </div>
</div>
